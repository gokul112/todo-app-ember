{{page-title "Todos"}}


<h1>Todo App</h1>

<input {{on "input" this.updateNewTodoTitle}} value={{this.newTodoTitle}} type="text" placeholder="add new todo"/>

<button {{on "click" this.addNewTodo}} >Add New</button>

<ul>
{{#each this.todos as |todo|}}
<li>{{todo}}</li>
{{/each}}
</ul>

